# Minimal Configuration Example
# This is the simplest configuration file that overrides a single command

version = "1.0"

# Override the type-check command to use a different script name
[commands.type-check]
exec = "npm run typecheck"
description = "Run TypeScript type checker (custom script name)"
tech_stacks = ["npm", "pnpm", "yarn"]
